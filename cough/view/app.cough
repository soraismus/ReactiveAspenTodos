{ div } = require('../../vendor/reactive-aspen').React.DOM

AppBody   = require './body'
AppFooter = require './footer'
AppHeader = require './header'

TodoApp = \appState ->
  { bodyProps, footerProps } = appState

  div (
    null
    AppHeader ()
    AppBody bodyProps
    AppFooter footerProps
  )

module.exports = TodoApp
